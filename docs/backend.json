{
  "entities": {
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a product with its details.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of the product."
        },
        "unit": {
          "type": "string",
          "description": "Unit of the product (e.g., kg, piece)."
        },
        "gst": {
          "type": "number",
          "description": "GST percentage applicable to the product."
        },
        "partyName": {
          "type": "string",
          "description": "Name of the party associated with the product."
        },
        "pageNo": {
          "type": "number",
          "description": "Page number where the product is recorded."
        },
        "billDate": {
          "type": "string",
          "description": "Date of the bill for the product.",
          "format": "date-time"
        },
        "category": {
          "type": "string",
          "description": "Category of the product."
        }
      },
      "required": [
        "name",
        "unit",
        "gst",
        "partyName",
        "pageNo",
        "billDate",
        "category"
      ]
    },
    "Rate": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Rate",
      "type": "object",
      "description": "Represents a rate entry for a product.",
      "properties": {
        "rate": {
          "type": "number",
          "description": "The rate of the product at a specific time."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the rate was recorded, using server a timestamp.",
          "format": "date-time"
        }
      },
      "required": [
        "rate",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com",
      "password"
    ]
  },
  "firestore": {
      "collections": {
          "products": {
            "schema": { "$ref": "#/entities/Product" },
            "description": "Stores product documents.",
            "subcollections": {
              "rates": {
                "schema": { "$ref": "#/entities/Rate" },
                "description": "Stores the rate history for a product."
              }
            }
          }
      }
  }
}
